const Options = {
  bottom: "32px",
  right: "32px",
  left: "unset",
  time: "0.5s",
  mixColor: "#FFF",
  backgroundColor: "#FFF",
  buttonColorDark: "#100F2C",
  buttonColorLight: "#FFF",
  saveInCookies: true,
  label: "🌓",
  autoMatchOsTheme: true,
};

const DarkMode = new Darkmode(Options);
DarkMode.showWidget();
